Displaying System Information (uname, hostname, df, du).

# uname – System and kernel info
Example : uname -a.

# hostname – Show or set system hostname
Example: hostname (gives present host name we can change by using sudo command).

# df – Disk space usage
Example: df-h (in human readable format).

# du – Directory/file size
Example: du -sh /home/user/Downloads
