# Test Operators
Operator	Meaning
-eq	      Equal to
-ne	      Not equal to
-gt	      Greater than
-lt	      Less than
-ge	      Greater than or equal to
-le	      Less than or equal to

Example: a=5
         b=10
# Equal
if [ "$a" -eq "$b" ]; then
  echo "Equal"
else
  echo "Not equal"
fi

# Greater than
if [ "$b" -gt "$a" ]; then
  echo "$b is greater than $a"
fi

# Less than
if [ "$a" -lt "$b" ]; then
  echo "$a is less than $b"
fi
