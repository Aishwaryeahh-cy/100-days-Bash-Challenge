# grep
It is used to search for specific words, patterns, or lines inside files or command outputs.
Example: grep "apple" fruits.txt
         grep -i "apple" fruits.txt
         grep -v "apple" fruits.txt
         grep "error" *.log
         grep -r "apple" /home/user/
.
